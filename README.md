# Usage
`ENetBroadcastServer [PORT] [MAX_CLIENT_COUNT]`

Optionally, to have a different default port and/or max client count, you can change the default port and/or max client count at the top of `main.c` and then compile via script in `build/`.